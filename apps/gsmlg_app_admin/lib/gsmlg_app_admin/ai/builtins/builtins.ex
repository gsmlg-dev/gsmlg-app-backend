defmodule GsmlgAppAdmin.AI.Builtins do
  @moduledoc """
  Namespace for built-in tool handler functions.

  All builtin tool handlers must be defined under this namespace
  (e.g., `GsmlgAppAdmin.AI.Builtins.DateTime`) to be callable by
  the ToolExecutor. This restriction prevents arbitrary code execution.
  """
end
