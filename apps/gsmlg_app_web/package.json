{
  "name": "gsmlg-web",
  "version": "1.0.0",
  "private": true,
  "description": "GSMLG App Website",
  "main": "priv/static/assets/app.js",
  "scripts": {
    "build": "bun build assets/js/app.js --outdir=priv/static/assets --target=browser --format=esm",
    "build:deploy": "bun build assets/js/app.js --outdir=priv/static/assets --target=browser --format=esm --minify",
    "dev": "bun build assets/js/app.js --outdir=priv/static/assets --target=browser --format=esm --watch",
    "test": "echo 'no test'"
  },
  "author": "Jonathan Gao",
  "license": "ISC",
  "dependencies": {
    "@duskmoon-dev/core": "^1.16.0",
    "@duskmoon-dev/css-art": "^1.16.0",
    "@duskmoon-dev/elements": "^1.1.1",
    "@duskmoon-dev/art-elements": "^1.1.1"
  },
  "devDependencies": {
    "@tailwindcss/typography": "^0.5.16"
  }
}